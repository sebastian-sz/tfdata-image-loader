from setuptools import setup, find_packages

setup(
    name="tfdata_image_loader",
    version="0.1",
    packages=find_packages(),
    description="A custom module for loading image data for Image Classification problem.",
    author="Sebastian Sz",
)
